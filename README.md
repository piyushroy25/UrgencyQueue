# UrgencyQueue
A Java implementation of a priority queue that orders elements by urgency using generics and linked nodes. Supports enqueue, dequeue, and peek operations with stability and custom comparator logic.
